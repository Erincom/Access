<%= form_tag(attendance_report_path, :method => "get", class: 'inline-form') do %>
<%= select_tag :company, options_for_select(Student.list_companies), {include_blank: false, class: 'select select_company' } %>
<%= select_tag :session, options_from_collection_for_select(Session.order('sessionstartdate DESC'), :sessionid, :sessionname), {include_blank: false, class: 'select select_metaclassname' }  %>
<!-- <div class="blank-space"></div> -->
<%= submit_tag "Go", class: "btn btn-primary homepage-button" %>
<% end %>
